import { PageSection } from "@/components";

export default function StockPage() {
  return (
    <PageSection title="Stock" description="Controle de inventário e ingredientes">
      <div className="rounded-[32px] bg-surface p-6 shadow-sm">
        <h1 className="text-xl font-semibold">Stock — em construção</h1>
      </div>
    </PageSection>
  );
}
